import { PedidoController } from './controllers/PedidoController.js';

document.addEventListener("DOMContentLoaded", () => {
    // Agora o sistema é puramente inicializado via Controller
    new PedidoController();
});
